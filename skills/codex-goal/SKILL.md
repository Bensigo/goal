---
name: codex-goal
description: Compatibility alias for the Goal loop. Use when the user says codex goal, /goal, goal loop, Ralph loop, RLAPH loop, keep working until done, or asks for autonomous worker/reviewer instances.
---

# Codex Goal Compatibility Alias

Use the neutral `goal` skill instead:

```bash
goal start-ready "<execution handoff including Definition of Done and Verification Plan>"
```

The `codex-goal` command remains as a compatibility wrapper around `goal`.
